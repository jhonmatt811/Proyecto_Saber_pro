pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url 'https://repo.spring.io/release' }  // Agregar repositorio de Spring Plugins
    }
}

rootProject.name = 'demo'
